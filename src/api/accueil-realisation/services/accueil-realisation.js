'use strict';

/**
 * accueil-realisation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::accueil-realisation.accueil-realisation');
