'use strict';

/**
 * accueil-realisation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::accueil-realisation.accueil-realisation');
